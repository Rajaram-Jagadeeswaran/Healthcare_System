from django.apps import AppConfig

class HospitalConfig(AppConfig):
    """
    AppConfig for the 'hospital' app.

    This class is used to configure the 'hospital' app and includes metadata
    such as the app's name.
    """
    name = 'hospital'
    